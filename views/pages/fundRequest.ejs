<% include ../partials/top %>

<div id="main" class="alt">

		<!-- One -->
			<section id="one">
				<div  onclick="location.href='/project/<%= projectId %>';" class="inner">
					<header class="major">
						<h1><%=title%></h1>
					</header>
					<span class="image main"><img src="images/pic11.jpg" alt="" /></span>
					<p><%=application%></p>
				</div>
				<!-- show documents -->
				<%if(status==='pending') { %>
					<div>
						<button onclick="window.location.href='/employee/<%=req_id%>/approve-permission?projectId=<%=projectId%>'" class="btn btn-success">Approve</button>
						<button class="btn btn-danger">Deny</button>
					</div>
						
				<%}%>
			</section>

	</div>

<% include ../partials/footer %>